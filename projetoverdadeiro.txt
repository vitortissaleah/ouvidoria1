Lista = []
opcao = 3
remover = 0
codigo = ''
while opcao != 5:
  print('OUVIDORIA UNIFACISA')
  print('Opções:')
  print()
  print('1) Listar reclamações.')
  print('2) Adicionar nova reclamação.')
  print('3) Remover uma reclamação.')
  print('4) Pesquisar uma reclamação pelo código. ')
  print('5) Sair.')
  print()
  
  opcao = int(input('Digite sua opção: '))
  print()
  
  if opcao == 1:

    quantidadedeLista = len(Lista)
    if quantidadedeLista == 0:
      print("-Não existem reclamações.")
      print()
   
    else:
      print("LISTA DE RECLAMAÇÕES: ")
      for i in range(len (Lista) ):
        print(i+1,')',Lista[i] ) 
      print()
        
  elif opcao == 2:
    print()
    NovaReclamacao = input("adicionar reclamação: ")
    Lista.append(NovaReclamacao)

    for i in range(len(Lista)):
      
      print()
      print(i+1,')', Lista[i])
      print("adicionado com sucesso! ")
      print()
    
  elif opcao == 3:
    
    quantidadedeLista = len(Lista)
    
    if quantidadedeLista == 0:
      print("Não existem reclamações para serem removidas.")
      print()
    
    else:
      print("LISTA DE RECLAMAÇÕES: ")
      print()
      
      for i in range(len(Lista)):
        print(i+1,')',Lista[i] ) 
      print()
     
      remover = int(input("Digite o código da reclamação que você quer remover: "))
    
      while remover <= 0 or remover > len(Lista):
          print()
          print("Codigo inválido.")
          print()
      
          remover = int(input("Digite o código da reclamação que você quer remover: "))
      
      del Lista [remover-1]
      
      print()
      print ('-Removido com Sucesso.')
      print()
#Se a opção for (4), ele vai pesquisar as reclamações pelo codigo se houver reclamações , e se código for invalido
  elif opcao ==4:
    
    quantidadedeLista = len(Lista)
    
    if quantidadedeLista == 0:
      print("Não existe reclamações.")
      print()
   
    elif quantidadedeLista  > 0:
      
        codigo = int(input("Pesquise pelo codigo: "))
        
        while codigo <= 0 or codigo > len(Lista):
          print()
          print("Codigo inválido.")
          print()
          codigo = int(input("pesquise pelo codigo: "))

        else:
          reclamacaoPesquisada = Lista[codigo-1]
          print()
          print('Sua reclamação encontrada:', 
          reclamacaoPesquisada)
          print()
  
  elif opcao == 5:
    print()
    break

  elif opcao > 5:
    print("Não existe essa opção, tente novamente.")
    print()

print ('Obrigado, volte sempre.')
